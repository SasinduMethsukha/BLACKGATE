# Stage 04 — The Quiet Log

Inspect `server.log`. Establish the normal request pattern and then investigate the DEBUG records around the incident time.

The PCAP reference is intentionally visible as a clue. One field is Base64 encoded.
